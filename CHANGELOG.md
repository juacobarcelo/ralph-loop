# Changelog

## 0.1.0

- Initial implementation of core models and state machine
- Backend abstraction for Codex, Copilot, Claude, and Docker sandbox
- CLI orchestration commands (`next-action`, `update`, `run`, `execute`, `inspect`, `init`, etc.)
- Verification pipeline (deterministic + AI inspection + visual stub)
- Docker-first workflow and CI scaffolding
