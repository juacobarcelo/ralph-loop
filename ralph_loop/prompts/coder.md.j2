# Task: {{ task_title }}

## Description
{{ task_description }}

## Acceptance Criteria
{% for criterion in acceptance_criteria %}
{{ loop.index }}. {{ criterion }}
{% endfor %}

{% if reference_impl %}
## Reference Implementation
{{ reference_impl }}
{% endif %}

## Constraints
{% for constraint in constraints %}
- {{ constraint }}
{% endfor %}
{% for file_path in files_not_to_touch %}
- Do NOT modify `{{ file_path }}`
{% endfor %}

{% if contract_content %}
## Implementation Contract
{{ contract_content }}
{% endif %}

{% if project_instructions %}
## Project Instructions
{{ project_instructions }}
{% endif %}

{% if capabilities %}
## Available Capabilities
{% for capability in capabilities %}
- `{{ capability.id }}` ({{ capability.type }}): {{ capability.instruction }}
{% endfor %}
{% endif %}

{% if is_retry %}
## ⚠️ RETRY — Attempt {{ attempt_number }} of {{ max_retries }}

This task failed on previous attempts. Fix all issues from previous feedback before declaring done.
Run `git diff` first to inspect previously rejected edits, then correct the implementation according to review findings.

{{ previous_feedback }}
{% endif %}

## Instructions
1. Implement the task end-to-end for code changes only.
2. Keep changes scoped to this task.
3. Current flow is `coder -> reviewer -> update -> commit`; do not invent or assume extra pipeline stages.
4. Use only the capabilities explicitly listed above; if none are listed, do not assume MCP/browser tooling is available.
5. Do not run host-only commands such as `docker` or `docker compose` from inside the coding container unless the task explicitly requires it.
