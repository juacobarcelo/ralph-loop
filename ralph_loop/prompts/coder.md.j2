# Task: {{ task_title }}

## Description
{{ task_description }}

## Acceptance Criteria
{% for criterion in acceptance_criteria %}
{{ loop.index }}. {{ criterion }}
{% endfor %}

## Files to Create/Modify
{% for file_path in files_to_touch %}
- `{{ file_path }}`
{% endfor %}

{% if reference_impl %}
## Reference Implementation
{{ reference_impl }}
{% endif %}

## Constraints
{% for constraint in constraints %}
- {{ constraint }}
{% endfor %}
{% for file_path in files_not_to_touch %}
- Do NOT modify `{{ file_path }}`
{% endfor %}

{% if contract_content %}
## Implementation Contract
{{ contract_content }}
{% endif %}

{% if project_instructions %}
## Project Instructions
{{ project_instructions }}
{% endif %}

{% if is_retry %}
## ⚠️ RETRY — Attempt {{ attempt_number }} of {{ max_retries }}

This task failed on previous attempts. Fix all issues from previous feedback before declaring done.

{{ previous_feedback }}
{% endif %}

## Instructions
1. Implement the task end-to-end for code changes only.
2. Keep changes scoped to this task.
3. You are part of the coding step only. Testing and verification are handled by separate ralph-loop stages/agents.
4. During `code`, do not run tests or visual verification tooling (for example: `jest`, `tsc`, `playwright`, screenshot checks, or `visual_verify` commands).
5. `verify_commands` are executed by the ralph-loop `verify` step on the host. During `code`, do not run host-only commands such as `docker` or `docker compose` from inside the coding container.
